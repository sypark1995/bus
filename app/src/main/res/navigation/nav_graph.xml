<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.sypark.bus.MainFragment"
        android:label="MainFragment"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_mainFragment_to_searchBusFragment"
            app:destination="@id/searchBusFragment" />
    </fragment>
    <fragment
        android:id="@+id/searchBusFragment"
        android:name="com.sypark.bus.SearchBusFragment"
        android:label="SearchBusFragment"
        tools:layout="@layout/fragment_search_bus" />
</navigation>